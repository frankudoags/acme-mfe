{
  "name": "@acme/shell",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5005 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5005 --strictPort"
  },
  "dependencies": {
    "@acme/packages": "workspace:*"
  }
}